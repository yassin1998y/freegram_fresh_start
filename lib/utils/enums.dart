// lib/utils/enums.dart

/// Time filter options for trending posts
enum TimeFilter {
  today,
  thisWeek,
  allTime;
}

/// Feed type options for different feed views
enum FeedType {
  following,
  trending,
  nearby;
}
