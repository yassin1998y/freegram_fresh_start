import 'dart:ui';

class RandomChatConfig {
  static const maxChatDuration = Duration(minutes: 5);
  static const pipWindowSize = Size(110, 160);
  static const searchTimeout = Duration(seconds: 15);
  static const historySaveThreshold = Duration(seconds: 5);
}
